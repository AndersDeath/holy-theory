import { buildHeadline } from "./buildHeadline";
import { buildLink } from "./buildLink";
import { buildLinksList } from "./buildLinksList";
import { buildListItem } from "./buildListItem";
import { htmlPageWrapper } from "./htmlPageWrapper";
import { wrapToHtmlTag } from "./wrapToHtmlTag";
export {
  buildHeadline,
  buildLink,
  buildLinksList,
  buildListItem,
  htmlPageWrapper,
  wrapToHtmlTag
};
